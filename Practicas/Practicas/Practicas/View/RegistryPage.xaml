<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Practicas.View.RegistryPage"
             Title="Registro">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Image Source="logo.png"/>
                <Entry Placeholder="Name"  Text="{Binding User.Name}"/>
                <Entry Placeholder="Email" Keyboard="Email"  Text="{Binding User.Email}"/>
                <Entry Placeholder="Password" Text="{Binding User.Password}" IsPassword="True"/>
                <Entry Placeholder="Confirm Password" Text="{Binding ConfirmPassword}" IsPassword="True"/>
                <Button Text="Register" Command="{Binding RegisterCommand}"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>