<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Practicas.View.DetailPage"
             Title="Contact Detail Page">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <Entry x:Name="ContactFirstName" VerticalOptions="Start" Text="{Binding Path=Contact.FirstName}"/>

            <Entry x:Name="ContactLastName" VerticalOptions="Start" Text="{Binding Contact.LastName}"/>

            <Entry x:Name="ContactCompany" Placeholder="Name" VerticalOptions="Start" Text="{Binding Contact.Company}"/>

            <Entry x:Name="ContactPhoneNumber" Placeholder="Name" VerticalOptions="Start" Text="{Binding Contact.Phone}"/>

            <Entry x:Name="ContactMobileNumber" Placeholder="Number" Keyboard="Numeric"  VerticalOptions="Start" Text="{Binding Contact.Mobile}"/>

            <Entry x:Name="ContactEmail" Placeholder="Number" Keyboard="Email"  VerticalOptions="Start" Text="{Binding Contact.Email}"/>

            <Entry x:Name="ContactDirection" Placeholder="Number" VerticalOptions="Start" Text="{Binding Contact.Direction}"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>