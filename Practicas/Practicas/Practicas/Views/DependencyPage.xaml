<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:control="clr-namespace:Practica.Controls"
             xmlns:local="clr-namespace:Practica.Effects"
             mc:Ignorable="d"
             x:Class="Practica.Views.DependencyPage"
             BackgroundColor="AliceBlue"
             >
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand"   HorizontalOptions="FillAndExpand">
            <Entry>
                <Entry.Effects>
                    <local:FocusEffect/>
                </Entry.Effects>
            </Entry>
            <Label Text="{Binding DeviceOrientation}"/>
            <Button 
                Text="Orientation"
                Command="{Binding OrientationCommand}"/>
            <control:CustomEntry HasCustomColor="False"/>
            <control:CustomEntry HasCustomColor="True"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>