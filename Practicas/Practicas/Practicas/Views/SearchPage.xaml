<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Practicas.Views.SearchPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Make API Call"
                VerticalOptions="StartAndExpand" 
                HorizontalOptions="Start" 
                Command="{Binding GetComments}"/>

            <ListView ItemsSource="{Binding Snippets}"
                  x:Name="listv">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding authorDisplayName}"/>
                                <Label Text="{Binding textDisplay}"/>
                                <Label Text="{Binding publishedAt}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>